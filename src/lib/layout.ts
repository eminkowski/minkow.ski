/** Shared layout tokens for responsive pages. */
export const PAGE_MAX_WIDTH = 'w-full max-w-3xl'

/** Main content horizontal padding. */
export const PAGE_PADDING_X = 'px-4 sm:px-6'

/** Minimum comfortable touch target (WCAG 2.5.5). */
export const TOUCH_TARGET = 'min-h-11'
